{
  "name": "uswds-tutorial",
  "private": true,
  "author": "USWDS",
  "license": "CC0-1.0",
  "scripts": {
    "federalist": "",
    "postinstall": "npm run jekyll:install",
    "jekyll:install": "gem install bundler && bundle install",
    "jekyll:serve": "bundle exec jekyll serve --livereload --incremental --host=localhost",
    "start": "npm run jekyll:serve",
    "test": "snyk test"
  },
  "dependencies": {
    "@uswds/compile": "github:uswds/uswds-gulp#jm-gulp-4",
    "snyk": "^1.704.0",
    "uswds": "^2.13.0"
  }
}
